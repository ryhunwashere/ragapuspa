---
import BaseLayout from "@layouts/BaseLayout.astro";

import SplitText from "@components/index/SplitText.svelte";
import LinkButton from "@components/global/LinkButton.svelte";

const title = "Home";
---

<BaseLayout title={title}>
  <section class="hero">
    <div class="hero-video-wrapper">
      <video class="hero-video" autoplay muted loop crossorigin="anonymous">
        <source
          src="https://res.cloudinary.com/djow2j0qk/video/upload/v1765122068/Shizuha_Loop_g6tx38.webm"
          type="video/webm"
        />
      </video>
      <div class="hero-overlay"></div>
      <div class="hero-caption">
        <SplitText text="Ragapuspa" client:load />
      </div>
      <div class="music-button-container">
        <LinkButton href="/music" text="Music" />
      </div>
    </div>

    <div class="hero-slogan">
      <h1>Like-minded creative people create together.</h1>
    </div>
    <div class="hero-description">
      <p>
        We are a creative group from Indonesia that consists of skilled artists
        from multiple disciplines such as music, art, animation, and web design.
      </p>
    </div>
  </section>
  <a href="/album1">Album 1 Page</a>

  <div style="height: 500px;"></div>
</BaseLayout>

<style>
  .hero {
    width: 100%;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero-video-wrapper {
    position: relative;
    width: 100%;
    height: auto;
    display: flex;
    justify-content: center;
    overflow: hidden;
    align-items: center;
  }

  .hero-video {
    width: 100%;
    height: auto;
    max-height: 100vh;
    object-fit: cover;
    object-position: center;
    opacity: 0;
    animation: fadeIn 0.5s ease forwards;
  }

  .hero-overlay {
    position: absolute;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.4);
    pointer-events: none;
  }

  .hero-caption {
    position: absolute;
    display: block;
    color: rgb(255, 255, 255);
    z-index: 10;
    font-family: "Zen Maru Gothic", sans-serif;
    font-size: 8rem;
    font-style: normal;
    text-shadow: 6px 4px 9px black;
    line-height: 1;
    transform: translateY(-15vh);
  }

  .music-button-container {
    transform: translateY(15vh);
    position: absolute;
    opacity: 0;
    animation: fadeIn 0.5s ease forwards;
    animation-delay: 3s;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }

  .hero-title,
  .hero-slogan,
  .hero-description {
    text-align: left;
    width: 100%;
    max-width: 960px;
    margin: 1rem auto;
    padding: 0 1rem;
  }

  @media (max-width: 1024px) {
    .hero-video-wrapper {
      width: 100%;
      height: 100vh;
      overflow: hidden;
    }

    .hero-video {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .hero-caption {
      transform: translateY(-15vh);
      font-size: 8rem;
    }

    .hero-slogan,
    .hero-description {
      font-size: larger;
    }

    .music-button-container {
      transform: translateY(5vh);
    }
  }

  @media (max-width: 767px) {
    .hero-caption {
      font-size: 6rem;
    }
  }

  @media (max-width: 541px) {
    .hero-caption {
      font-size: 4rem;
    }
  }
</style>
